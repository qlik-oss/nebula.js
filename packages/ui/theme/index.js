import {
  ThemeProvider,
  useTheme,
  StylesProvider,
  createGenerateClassName,
  makeStyles,
} from '@material-ui/styles';

import createTheme from './create';

export {
  createTheme,
  useTheme,
  makeStyles,
  ThemeProvider,
  StylesProvider,
  createGenerateClassName,
};
