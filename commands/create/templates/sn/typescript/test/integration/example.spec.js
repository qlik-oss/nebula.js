import { test, expect } from '@playwright/test';

test.describe('<%= name %> chart', () => {
  test('should render without errors', async () => {
    // Add your integration tests here
    expect(true).toBe(true);
  });
});
